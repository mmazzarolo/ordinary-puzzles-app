appId: com.mmazzarolo.ordinarypuzzles
name: Undo and continue keep a board usable
tags:
  - interaction
  - regression
---
# Auto-solve is off here, so the board stays unsolved and both controls stay
# reachable. Undo with nothing committed is a quiet no-op.
- runFlow: ../subflows/launch-without-auto-solve.yaml
- runFlow:
    file: ../subflows/open-mode.yaml
    env:
      MODE: medium
- runFlow: ../subflows/assert-game.yaml
- tapOn:
    id: "button-undo"
- assertVisible:
    id: "puzzle-board"
- tapOn:
    id: "button-menu"
- runFlow: ../subflows/assert-home.yaml

# The board is persisted now, so home offers to continue it.
- assertVisible:
    id: "button-continue"
- tapOn:
    id: "button-continue"
- extendedWaitUntil:
    visible:
      id: "screen-game"
    timeout: 20000
- assertVisible:
    id: "puzzle-board"
